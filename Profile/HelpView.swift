import SwiftUI

struct HelpView: View {
    var body: some View {
        Text("Здесь будет раздел 'Помощь'")
            .font(.largeTitle)
            .padding()
    }
}

struct HelpView_Previews: PreviewProvider {
    static var previews: some View {
        HelpView()
    }
}
